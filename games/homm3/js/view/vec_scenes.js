/* ============================================================================
   view/vec_scenes.js — рисованные картины интерфейса: начало недели, уровень героя, победа, поражение, главное меню.
   ========================================================================== */
(function (root) {
  'use strict';
  const H3 = root.H3, V = H3 && H3.Vec, K = H3 && H3.VK; if (!V || !K) return;
})(typeof window !== 'undefined' ? window : globalThis);
