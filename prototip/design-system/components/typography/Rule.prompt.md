The 64x2px sage bar under a headline — Threshold's only ornament; it marks "headline ends, explanation begins".

```jsx
<Display size="screen">Your path</Display>
<Rule />
<BodyText>Observing · day 14</BodyText>
```

`dark` switches to sage-300 on forest surfaces. Never colour it, never thicken it, never use more than one per block.
