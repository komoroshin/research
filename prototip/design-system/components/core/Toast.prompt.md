Forest toast pinned 8px below the top safe area — confirms that something was recorded, and never celebrates.

```jsx
<Toast show={saved}>Day 5 counted · sleep 7 h 10 min</Toast>
```

Fades in and out over 200ms, auto-dismisses after 1.8s, never carries a button. Copy is factual: "Meal saved", "Day 12 updated".
